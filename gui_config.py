# GUI Configuration Settings
MAINWINDOW_SIZE_X = 1200
MAINWINDOW_SIZE_Y = 800
FOLDERS_ONLY = False # A boolean indicating whether to include files in the scan results.
STATIC_SCROLLABLE_AREA = True # A boolean indicating whether the scrollable area is static or unique for each item.